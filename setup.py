from setuptools import find_packages, setup

setup(
    name='simcoder',
    packages=find_packages(),
    version='0.1.0',
    description='Runs a SimCLR encoding over an image set.',
    author='David Morrison'
)